/*
// Request Body에 사용되는 타입
export type PostBody = {
  category: string;
  title: string;
  body: string;
};

// 서버에서 데이터를 받아올 때 사용하는 타입
export type Post = {
  id: number;
  category: string;
  title: string;
  body: string;
  user_id: number;
  username: string;
  views: number;
  comments: number;
  recommendations: number;
  date: Date;
};
*/
