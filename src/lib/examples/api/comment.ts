/*
export const getCommentList = (postId: number) => {
  return fetch(`${process.env.API_BASE_URL}/api/comment?postId=${postId}`, {
    method: "GET",
    cache: "force-cache",
    next: {
      tags: ["getCommentList"],
    },
  });
};
*/
